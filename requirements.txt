Pillow>=10.0
python-dotenv>=1.0
psycopg2-binary>=2.9
sentence-transformers>=3.0
numpy>=1.24
pandas>=2.0

# ── RAM++ 키워드 추출 ──
torch>=2.0
torchvision>=0.15
timm>=1.0
fairscale>=0.4
ram @ git+https://github.com/xinyu1205/recognize-anything.git
huggingface-hub>=0.20

# ── S3 업로드 ──
boto3>=1.28

# ── Moondream 2 캡션 생성 (transformers + trust_remote_code) ──
transformers>=4.40,<4.45.0
accelerate>=0.26
einops>=0.8

# ── Agent Router (LLM 의도 분류) ──
openai>=1.0

# ── JWT 인증 ──
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# ── FastAPI 추가 ──
fastapi>=0.100
uvicorn>=0.20
python-multipart>=0.0.6
email-validator>=2.0
